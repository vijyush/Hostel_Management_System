import 'package:flutter/material.dart';

class GuardDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("GUARD Dashboard")),
      body: Center(child: Text("Welcome, Student!")),
    );
  }
}
